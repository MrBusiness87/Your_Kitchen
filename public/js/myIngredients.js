const searchButton = $("#searchButton");
const ingredientsDiv = $("#ingredients"); //ingredients div for display

searchButton.on("click", (event) => {
  event.preventDefault();
  //When search button is click, this happens
});
